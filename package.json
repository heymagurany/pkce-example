{
  "name": "genesys-pkce-example",
  "version": "1.0.0",
  "description": "Example project for using the authorization code/PKCE extension to get an access token for Genesys Cloud",
  "private": true,
  "scripts": {
    "build": "make build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "make build && webpack serve & npm-watch"
  },
  "watch": {
    "build": "src/*.*"
  },
  "author": "Matt Magurany",
  "license": "MIT",
  "dependencies": {
    "genesys-spark-components": "^4.81.2"
  },
  "devDependencies": {
    "npm-watch": "^0.13.0",
    "webpack": "^5.94.0",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^5.1.0"
  }
}
